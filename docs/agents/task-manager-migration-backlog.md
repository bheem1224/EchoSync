# Task Manager & JobQueue Migration Backlog

**Generated By:** `scripts/audit_task_manager_adoption.py`  
**Total Findings:** 12 (0 CRITICAL, 0 MEDIUM, 12 LOW)  
**Governing ADR:** [ADR 0001: Two-Tier Worker Pools, SQLite Write Serialization, and Task Lifecycle Management](file:///c:/Users/bheem/VScode-Projects/EchoSync/docs/agents/adrs/0001-task-manager-and-worker-pools.md)  

---

## 1. Executive Summary & Audit Matrix

| Subsystem | Critical (Threads/Processes) | Medium (Unleased Writes) | Low (Polling/Async) | Total |
| :--- | :--- | :--- | :--- | :--- |
| `core/` | 0 | 0 | 4 | 4 |
| `plugins/` | 0 | 0 | 1 | 1 |
| `services/` | 0 | 0 | 4 | 4 |
| `web/` | 0 | 0 | 3 | 3 |

---

## 2. Critical Concurrency Migrations (Unmanaged Threads & Processes)

These call sites spawn raw OS threads or sub-processes outside the Task Manager's two-tier pools and must be migrated immediately.

| File | Line | Symbol | Details | Action Required |
| :--- | :--- | :--- | :--- | :--- |

---

## 3. Medium Severity: Unleased Database Writes

These functions execute database mutations against SQLite without acquiring `with job_queue.db_write_lease():`, risking database lock collisions under concurrent workloads.

| File | Line | Context | Code Snippet | Action Required |
| :--- | :--- | :--- | :--- | :--- |

---

## 4. Low Severity: Custom Polling Loops & Detached Async Tasks

| File | Line | Symbol | Details | Recommendation |
| :--- | :--- | :--- | :--- | :--- |
| [`core/error_handler.py:51`](file:///c:/Users/bheem/VScode-Projects/EchoSync/core/error_handler.py#L51) | L51 | `handle_exception (while-sleep loop)` | Ad-hoc polling loop detected with sleep call; risk of busy-wait or drift. | Replace custom polling loop with a periodic ScheduledJob in JobQueue. |
| [`core/error_handler.py:84`](file:///c:/Users/bheem/VScode-Projects/EchoSync/core/error_handler.py#L84) | L84 | `handle_exception (while-sleep loop)` | Ad-hoc polling loop detected with sleep call; risk of busy-wait or drift. | Replace custom polling loop with a periodic ScheduledJob in JobQueue. |
| [`core/rate_limiter.py:65`](file:///c:/Users/bheem/VScode-Projects/EchoSync/core/rate_limiter.py#L65) | L65 | `wait (while-sleep loop)` | Ad-hoc polling loop detected with sleep call; risk of busy-wait or drift. | Replace custom polling loop with a periodic ScheduledJob in JobQueue. |
| [`core/rate_limiter.py:86`](file:///c:/Users/bheem/VScode-Projects/EchoSync/core/rate_limiter.py#L86) | L86 | `wait (while-sleep loop)` | Ad-hoc polling loop detected with sleep call; risk of busy-wait or drift. | Replace custom polling loop with a periodic ScheduledJob in JobQueue. |
| [`services/download_manager.py:700`](file:///c:/Users/bheem/VScode-Projects/EchoSync/services/download_manager.py#L700) | L700 | `_process_loop (while-sleep loop)` | Ad-hoc polling loop detected with sleep call; risk of busy-wait or drift. | Replace custom polling loop with a periodic ScheduledJob in JobQueue. |
| [`services/download_manager.py:792`](file:///c:/Users/bheem/VScode-Projects/EchoSync/services/download_manager.py#L792) | L792 | `asyncio.create_task` | Detached asyncio background task spawned without supervisor registration. | Ensure task lifecycle and cancellation are tracked via ProcessSupervisor. |
| [`services/download_manager.py:1436`](file:///c:/Users/bheem/VScode-Projects/EchoSync/services/download_manager.py#L1436) | L1436 | `asyncio.create_task` | Detached asyncio background task spawned without supervisor registration. | Ensure task lifecycle and cancellation are tracked via ProcessSupervisor. |
| [`services/download_manager.py:1474`](file:///c:/Users/bheem/VScode-Projects/EchoSync/services/download_manager.py#L1474) | L1474 | `asyncio.create_task` | Detached asyncio background task spawned without supervisor registration. | Ensure task lifecycle and cancellation are tracked via ProcessSupervisor. |
| [`web/routes/jobs.py:296`](file:///c:/Users/bheem/VScode-Projects/EchoSync/web/routes/jobs.py#L296) | L296 | `event_generator (while-sleep loop)` | Ad-hoc polling loop detected with sleep call; risk of busy-wait or drift. | Replace custom polling loop with a periodic ScheduledJob in JobQueue. |
| [`web/routes/library.py:499`](file:///c:/Users/bheem/VScode-Projects/EchoSync/web/routes/library.py#L499) | L499 | `event_generator (while-sleep loop)` | Ad-hoc polling loop detected with sleep call; risk of busy-wait or drift. | Replace custom polling loop with a periodic ScheduledJob in JobQueue. |
| [`web/routes/system_tasks.py:108`](file:///c:/Users/bheem/VScode-Projects/EchoSync/web/routes/system_tasks.py#L108) | L108 | `event_generator (while-sleep loop)` | Ad-hoc polling loop detected with sleep call; risk of busy-wait or drift. | Replace custom polling loop with a periodic ScheduledJob in JobQueue. |
| [`plugins/EchoSync/musicbrainz/client.py:822`](file:///c:/Users/bheem/VScode-Projects/EchoSync/plugins/EchoSync/musicbrainz/client.py#L822) | L822 | `asyncio.create_task` | Detached asyncio background task spawned without supervisor registration. | Ensure task lifecycle and cancellation are tracked via ProcessSupervisor. |

---

## 5. Phased Migration Roadmap

### Phase 1: High-Impact Background Services
1. **`services/download_manager.py`:** Migrate thread pool runner to `JobQueue` general pool with scoped `db_write_lease()`. 
2. **`services/library_watcher.py`:** Wrap filesystem event debounce writes with `db_write_lease()`. 
3. **`services/media_manager.py`:** Route background scan workers through `ProcessSupervisor`. 

### Phase 2: Plugin Ingress & Provider Workers
1. Standardize plugin background loops in `plugins/EchoSync/` to use `self.sdk.jobs` or `job_queue.register_job()`. 
2. Ensure all plugin database writes route via `self.aliases`, `self.attributes`, or `db_write_lease()`. 

### Phase 3: Web Route Background Tasks
1. Ensure FastAPI `BackgroundTasks` mutating canonical tracks acquire `db_write_lease()`. 