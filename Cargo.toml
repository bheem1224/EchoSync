[package]
name = "echosync_core"
version = "0.1.0"
edition = "2021"

[lib]
name = "echosync_core"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.21", features = ["extension-module"] }
walkdir = "2"
lofty = "0.21"
rusqlite = { version = "0.31", features = ["bundled"] }
