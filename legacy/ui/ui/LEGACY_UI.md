# Legacy PyQt UI

- The PyQt-based desktop UI in this `ui/` package (and `main.py`) is frozen and not used for the Svelte web UI deployment path.
- Keep for reference/visual parity only. Do not add new backend logic here; the active backend is the Flask API in `web/api_app.py`.
