dashboard:
  views:
    - id: manager
      title: Manager
      sidebar:
        enabled: true
        cards:
          - type: echosync-account-linker
      sections:
        - fullWidth: true
          cards:
            - type: echosync-manager-settings
        - fullWidth: true
          cards:
            - type: echosync-managed-accounts
        - cards:
            - type: echosync-duplicate-resolution
        - cards:
            - type: echosync-pending-actions
